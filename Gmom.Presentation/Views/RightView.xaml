<UserControl
    x:Class="Gmom.Presentation.Views.RightView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:Gmom.Presentation.Views"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <local:CustomerPanelView
            Grid.Column="0"
            Grid.Row="0"
            Margin="0,16,16,0" />

        <local:CartView Grid.Column="0" Grid.Row="2" />
    </Grid>
</UserControl>